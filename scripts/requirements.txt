# Only two third-party deps remain — both are pure-Python wheels with NO browser
# engine (no Playwright/Chromium). The other four sources (App Store, Reddit,
# Tinhte, Voz) use the Python standard library only, so they need no installs.
#
#   pip install -r requirements.txt
#
# Run with zero installs by dropping these two and relying on the fallback
# dataset for Google Play + YouTube (see references/sources.md).

google-play-scraper>=1.2.4   # Google Play reviews (internal Play API, no browser)
yt-dlp>=2024.1.1             # YouTube search + comments + transcripts (no browser)
langdetect>=1.0.9            # vi/en language qualification (pipeline.py)
